import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import type { CaseStudy } from "@/lib/case-studies";

export default function CaseStudyPage({ study }: { study: CaseStudy }) {
  return <main id="main-content" className="mx-auto min-h-screen max-w-6xl px-container pb-24 pt-36"><Link href="/#work" className="font-mono text-xs tracking-[.16em] text-muted-foreground underline underline-offset-4">Back to selected work</Link><header className="mt-12 max-w-4xl"><p className="font-mono text-xs tracking-[.16em] text-signal">{study.eyebrow}</p><h1 className="mt-5 text-5xl font-black tracking-tighter sm:text-7xl">{study.title}</h1>{study.signal && <p className="mt-6 inline-flex rounded-full border border-signal/60 px-4 py-2 font-mono text-xs text-signal">{study.signal}</p>}<p className="mt-7 text-xl leading-relaxed text-muted-foreground">{study.summary}</p><div className="mt-8 flex flex-wrap gap-3"><a href={study.repo} target="_blank" rel="noopener noreferrer" className="rounded-full border border-border px-5 py-3 text-sm hover:border-foreground">Source code <ArrowUpRight className="ml-1 inline h-4 w-4" /></a>{study.demo && <a href={study.demo} target="_blank" rel="noopener noreferrer" className="rounded-full bg-signal px-5 py-3 text-sm font-semibold text-black">Open demo <ArrowUpRight className="ml-1 inline h-4 w-4" /></a>}</div></header><div className="mt-16 grid gap-12 border-t border-border pt-12 md:grid-cols-2"><section><h2 className="text-2xl font-bold">Problem</h2><p className="mt-4 leading-relaxed text-muted-foreground">{study.problem}</p></section><section><h2 className="text-2xl font-bold">Approach</h2><p className="mt-4 leading-relaxed text-muted-foreground">{study.approach}</p></section><section><h2 className="text-2xl font-bold">Architecture</h2><ul className="mt-4 space-y-3 text-muted-foreground">{study.architecture.map((item) => <li key={item}>- {item}</li>)}</ul></section><section><h2 className="text-2xl font-bold">Engineering decisions</h2><div className="mt-4 space-y-5">{study.decisions.map((decision) => <div key={decision.title}><h3 className="font-semibold">{decision.title}</h3><p className="mt-1 leading-relaxed text-muted-foreground">{decision.detail}</p></div>)}</div></section></div><section className="mt-12 border-t border-border pt-10"><h2 className="text-2xl font-bold">Scope and limitations</h2><ul className="mt-4 space-y-3 text-muted-foreground">{study.limitations.map((item) => <li key={item}>- {item}</li>)}</ul><div className="mt-8 flex flex-wrap gap-2">{study.stack.map((item) => <span key={item} className="rounded-full border border-border px-3 py-2 text-xs">{item}</span>)}</div></section></main>;
}
